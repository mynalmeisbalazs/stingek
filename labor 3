#include <iostream>
#include <string>
using namespace std;

int main()
{
string str , szo;
   cout << "kerem a beolvasando stringet ";

   getline(cin,str);
      cout << "kerem a beolvasando stringet ";
         getline(cin,szo);
         size_t found = szo.find (" ");
         str.append(szo , 0 , found);
         cout << str;
    return 0;
}
